# Supabase project (Project Settings → API). Same values the Spring 2026 site uses
# are fine: the new tables do not collide with the archived ones.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: which row in public.events this deployment shows.
VITE_EVENT_SLUG=impact-miami-2
