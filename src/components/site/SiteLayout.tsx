import { Outlet } from "react-router-dom";

// STUB — replaced by the public-site agent.
const SiteLayout = () => (
  <div className="grid-ground min-h-screen">
    <Outlet />
  </div>
);
export default SiteLayout;
